var React = require('react');
var PropTypes = React.PropTypes;

var ExploreNavBar = React.createClass({

  render: function() {
    return (
      <div />
    );
  }

});

module.exports = ExploreNavBar;
